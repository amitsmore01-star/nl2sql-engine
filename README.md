# nl2sql-engine
A schema‑driven, multi‑app NL→SQL engine that converts natural language into validated, deterministic SQL using a hybrid LLM + rule‑based pipeline. Supports versioned schemas, hierarchy rules, business rules, and agent‑ready extensions.
